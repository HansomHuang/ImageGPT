# ImageGPT

Git repository initialized for this project.

## Workflow
- `git status` to inspect changes
- `git add <files>` to stage
- `git commit -m "message"` to record history
- Use branches and pull requests for feature work