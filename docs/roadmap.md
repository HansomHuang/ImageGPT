# Roadmap

## v1.1

- Improve RAW camera defaults (camera matrices + profile selection).
- Replace FastAPI startup events with lifespan API.
- Add explicit ICC profile selection UI for export.

## v1.2

- Add optional OpenEXR intermediate cache path for heavy exports.
- Add batch apply/export queue.
- Add side-by-side synchronized zoom controls in desktop preview.

## v2.0

- Optional GPU acceleration for preview path.
- Optional local masks with constrained scope (still color-focused).

