[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "imagegpt-backend"
version = "0.1.0"
description = "ImageGPT FastAPI backend"
requires-python = ">=3.11"
